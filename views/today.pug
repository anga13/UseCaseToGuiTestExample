extends index.pug
block content
  .employee-list
    ul
      for employee in employees
        li
          .employee(employee-id=`${employee.id}`)
            a(href=`/employees/${employee.id}`)= employee.name


